name: "Hello world"
description: "My first real add-on!"
version: "1.0.0"
slug: "hello_world"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386

# Build args for Supervisor
build:
  context: .
  dockerfile: Dockerfile
  args:
    BUILD_FROM: ghcr.io/home-assistant/amd64-base:latest

